# Railway Nixpacks configuration

[phases.setup]
nixPkgs = ['nodejs-22_x', 'pnpm']

[phases.install]
cmd = "pnpm install -r"

[phases.build]
cmd = "pnpm build"

[start]
cmd = "pnpm start"

[variables]
NODE_ENV = "production"